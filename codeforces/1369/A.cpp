#include <bits/stdc++.h>

using namespace std;

signed main() {
    int t; cin >> t;
    while (t--) {
        int a;
        cin >> a;
        if (a % 4 != 0) cout << "NO\n";
        else cout << "YES\n";
    }
}