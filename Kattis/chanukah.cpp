#include <bits/stdc++.h>

using namespace std;

signed main() {
    int t; cin >> t;
    while (t--) {
        int k,n; cin >> k >> n;
        cout << k << " " << n*(n+1)/2+n << "\n";
    }
    return 0;
}
